<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.thearbiter.thriftbooksnepal">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"></activity>
        <activity
            android:name=".Activities.MainALevelBuyer"
            android:label="@string/app_name"></activity>
        <activity
            android:name=".Activities.MainDrawerHome"
            android:label="@string/title_activity_main_drawer_home">

        </activity>
        <activity
            android:name=".Activities.SignUp"
            android:label="@string/title_activity_sign_up">

        </activity>
        <activity
            android:name=".Activities.Login"
            android:label="@string/title_activity_login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />

            </intent-filter>
        </activity>

        <activity
            android:name=".Activities.IbOptions"
            android:label="ib"></activity>
        <activity
            android:name=".Activities.ALevelOptions"
            android:label="alevel"></activity>

        <activity
            android:name=".Activities.PlusTwoOptions"
            android:label="ib"></activity>

        <activity
            android:name=".Activities.ActivitySeller"
            android:label="ib"></activity>
    </application>

</manifest>
